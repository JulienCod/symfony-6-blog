{{ form_start(userForm) }}
{{ form_row(userForm.email)}}
{{ form_row(userForm.firstName)}}
{{ form_row(userForm.lastName)}}

<button class="w-full text-white bg-gray-700 hover:bg-gray-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg
 text-sm px-5 py-2.5 mr-2 dark:bg-gray-700 border border-gray-300 dark:hover:bg-gray-400 focus:outline-none
  dark:focus:ring-gray-700" type="submit">
    Modifier le compte
</button>
{{ form_end(userForm) }}
